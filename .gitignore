# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Local state / secrets
.env
*.db
*.sqlite3
data/
.cache/
evidence/
out/
service-account*.json
firebase-*.json
.firebase/

# Node / dashboard
node_modules/
dashboard/dist/
dashboard/.env
dashboard/.env.local

# The dashboard's data snapshot. THIS REPOSITORY IS PUBLIC and this file is
# the whole catalog in plaintext -- listings, landed costs, margins. It is
# generated by `nippon-margin export` on every run and served only from behind
# Cloudflare Access. It must never be committed.
dashboard/public/data.json

# Editors / OS
.DS_Store
.idea/
.vscode/
